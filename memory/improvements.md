# 改进记录

## 2026-04-27
- **问题**: 在推进 OpenClaw + Hermes + Claude Code 整合时，容易把项目层、能力层、记忆层分离得太开，导致长期上下文没有真正进入工作流
- **教训**: 记忆系统不应该只是存档区，而应该成为整合项目的长期上下文层，持续记录架构思路、偏好、经验和约束
- **行动**: 保留现有 `memory/` 结构不乱拆，同时把整合思路明确写入 `memory/README.md`，让后续项目演进都能围绕这套长期记忆展开

## 2026-03-14
- **问题**: 配置Agent时输入错误的工作路径，导致目录创建失败
- **教训**: 在交互式CLI中使用特殊按键需要确认环境支持，直接修改配置文件更可靠
- **行动**: 以后优先使用配置文件修改，避免在交互式CLI中使用复杂操作

## 2026-03-14
- **问题**: 模型配置替换时没有保留旧模型，用户需要两个模型切换使用
- **教训**: 修改配置前应该确认用户需求，不要直接替换原有配置
- **行动**: 以后修改模型配置时先询问是否保留原有配置，默认添加新配置而不是替换

## 2026-06-09 - 进化会话
- **问题**: 这是一个全新会话，缺少上下文，需要重新建立与主人的连接和理解
- **教训**: 每次会话开始时应该主动加载核心记忆和身份信息，而不是被动等待
- **行动**: 
  1. 立即加载核心身份文件 (SOUL.md, IDENTITY.md, MEMORY.md)
  2. 从 SQLite 大脑加载关键记忆
  3. 主动询问主人当前需要继续什么工作
  4. 建立主动进化的机制，而不仅仅是被动响应