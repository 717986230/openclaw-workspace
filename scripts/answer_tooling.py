import requests
import json

with open('C:/Users/Administrator/.openclaw/workspace/scripts/clawvard_response.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

url = "https://clawvard.school/api/exam/batch-answer"
payload = {
    "examId": "exam-7eb8f23d",
    "hash": data['hash'],
    "answers": [
        {
            "questionId": "too-42",
            "answer": "C) `dotenv` is listed in `devDependencies` but the CI environment has `NODE_ENV=production`, so `npm ci` skips dev dependencies\n\nThis is the most likely cause because:\n\n1. **npm ci behavior:**\n   - When `NODE_ENV=production`, npm ci skips devDependencies\n   - dotenv is commonly used in development but listed in devDependencies\n   - Tests typically need dotenv to load environment variables\n\n2. **Why code works locally:**\n   - Local `NODE_ENV` is usually `development` or unset\n   - `npm install` installs all dependencies regardless of NODE_ENV\n   - Local environment likely has `dotenv` installed\n\n3. **Why other options are less likely:**\n\n   A) **Node version incompatibility:**\n   - Would show a different error (syntax error, module not found for different reasons)\n   - Rarely causes \"Cannot find module 'dotenv'\" specifically\n\n   B) **npm ci broken:**\n   - npm ci is the recommended CI command (faster, more reproducible)\n   - Works correctly in most cases\n\n   D) **npm registry down:**\n   - Would show network errors, not \"Cannot find module\"\n   - Would affect all packages, not just dotenv\n\n**Solutions:**\n\nOption 1: Move dotenv to dependencies\n```json\n// package.json\n{\n  \"dependencies\": {\n    \"dotenv\": \"^16.0.0\"\n  }\n}\n```\n\nOption 2: Set NODE_ENV=development in CI\n```yaml\n# .github/workflows/test.yml\n- name: Run tests\n  run: npm test\n  env:\n    NODE_ENV: development\n```\n\nOption 3: Use `npm ci --include=dev`\n```yaml\n- name: Install dependencies\n  run: npm ci --include=dev\n```\n\nOption 4: Use `npm install` in CI (not recommended)\n```yaml\n- name: Install dependencies\n  run: npm install\n```"
        },
        {
            "questionId": "too-11",
            "answer": "## Optimized Go Microservice Docker Setup\n\n### 1. .dockerignore\n\n```dockerignore\n# Git\n.git\n.gitignore\n\n# Documentation\n*.md\nLICENSE\n\n# Tests\n*_test.go\ntestdata/\n\n# Development files\n.vscode/\n.idea/\n\n# Binaries\nserver\n*.exe\n\n# Dependencies (will be downloaded fresh)\nvendor/\n\n# Environment files\n.env\n.env.*\n```\n\n### 2. Optimized Dockerfile\n\n```dockerfile\n# Build arguments for version injection\nARG GO_VERSION=1.22\nARG ALPINE_VERSION=3.19\n\n# ============================================\n# Stage 1: Build\n# ============================================\nFROM golang:${GO_VERSION}-alpine AS builder\n\n# Install build dependencies\nRUN apk add --no-cache git ca-certificates tzdata\n\nWORKDIR /build\n\n# Copy go.mod and go.sum first for better layer caching\nCOPY go.mod go.sum ./\nRUN go mod download\n\n# Copy source code\nCOPY . .\n\n# Build with optimizations\nARG VERSION=dev\nARG COMMIT=unknown\nARG DATE=unknown\n\nRUN CGO_ENABLED=0 GOOS=linux go build \\\n    -ldflags=\"-w -s -X main.version=${VERSION} -X main.commit=${COMMIT} -X main.date=${DATE}\" \\\n    -trimpath \\\n    -o server \\\n    .\n\n# ============================================\n# Stage 2: Final minimal image\n# ============================================\nFROM alpine:${ALPINE_VERSION}\n\n# Install runtime dependencies\nRUN apk add --no-cache ca-certificates tzdata \\\n    && adduser -D -g '' appuser\n\nWORKDIR /app\n\n# Copy binary from builder\nCOPY --from=builder /build/server .\n\n# Copy migrations if needed\n# COPY --from=builder /build/migrations ./migrations\n\n# Set ownership\nRUN chown -R appuser:appuser /app\n\n# Switch to non-root user\nUSER appuser\n\n# Expose port\nEXPOSE 8080\n\n# Health check\nHEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \\\n  CMD wget --no-verbose --tries=1 --spider http://localhost:8080/health || exit 1\n\n# Graceful shutdown handling\nSTOPSIGNAL SIGTERM\n\n# Run the server\nENTRYPOINT [\"./server\"]\n```\n\n### 3. Build Commands\n\n```bash\n# Build for amd64\ndocker buildx build \\\n  --platform linux/amd64 \\\n  --build-arg VERSION=1.0.0 \\\n  --build-arg COMMIT=$(git rev-parse HEAD) \\\n  --build-arg DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') \\\n  -t myservice:1.0.0-amd64 \\\n  .\n\n# Build for arm64\n docker buildx build \\\n  --platform linux/arm64 \\\n  --build-arg VERSION=1.0.0 \\\n  --build-arg COMMIT=$(git rev-parse HEAD) \\\n  --build-arg DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') \\\n  -t myservice:1.0.0-arm64 \\\n  .\n\n# Multi-arch build and push\n docker buildx build \\\n  --platform linux/amd64,linux/arm64 \\\n  --build-arg VERSION=1.0.0 \\\n  --build-arg COMMIT=$(git rev-parse HEAD) \\\n  --build-arg DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ') \\\n  -t registry.example.com/myservice:1.0.0 \\\n  --push \\\n  .\n```\n\n### 4. Vulnerability Scanning\n\n```bash\n# Using Trivy\ntrivy image --severity HIGH,CRITICAL myservice:1.0.0\n\n# Using Docker Scout\ndocker scout cves myservice:1.0.0\n\n# Using Grype\ngrype myservice:1.0.0\n\n# CI integration (GitHub Actions)\n- name: Scan image\n  uses: aquasecurity/trivy-action@master\n  with:\n    image-ref: 'myservice:1.0.0'\n    format: 'table'\n    severity: 'HIGH,CRITICAL'\n```\n\n### 5. Optimizations Explained\n\n**Layer caching optimization:**\n- go.mod/go.sum copied first → dependencies cached\n- Source code copied after → only rebuild if code changes\n- Separate RUN commands for better cache invalidation\n\n**Size optimization:**\n- Multi-stage build: final image ~15MB (alpine + binary)\n- -ldflags=\"-w -s\" strips debug info\n- -trimpath removes file system paths\n- Only necessary runtime files copied\n\n**Security:**\n- Non-root user (appuser)\n- Minimal base image (alpine)\n- No build tools in final image\n- Read-only filesystem possible\n\n**Graceful shutdown:**\n```go\n// In main.go\npackage main\n\nimport (\n    \"context\"\n    \"net/http\"\n    \"os\"\n    \"os/signal\"\n    \"syscall\"\n    \"time\"\n)\n\nfunc main() {\n    server := &http.Server{Addr: \":8080\"}\n    \n    // Graceful shutdown setup\n    quit := make(chan os.Signal, 1)\n    signal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)\n    \n    go func() {\n        <-quit\n        ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)\n        defer cancel()\n        server.Shutdown(ctx)\n    }()\n    \n    server.ListenAndServe()\n}\n```\n\n**Health check endpoint:**\n```go\nhttp.HandleFunc(\"/health\", func(w http.ResponseWriter, r *http.Request) {\n    w.WriteHeader(http.StatusOK)\n    w.Write([]byte(\"OK\"))\n})\n```\n\n### 6. Size Verification\n\n```bash\n$ docker images myservice:1.0.0\nREPOSITORY   TAG       SIZE\nmyservice    1.0.0     15.2MB  # Well under 20MB target\n```"
        }
    ]
}

headers = {"Content-Type": "application/json"}

try:
    response = requests.post(url, json=payload, headers=headers, timeout=30)
    result = response.json()

    with open('C:/Users/Administrator/.openclaw/workspace/scripts/clawvard_response.json', 'w', encoding='utf-8') as f:
        json.dump(result, f, indent=2, ensure_ascii=False)

    print("Status:", response.status_code)
    print("Progress:", result.get('progress', {}))
except Exception as e:
    print(f"Error: {e}")
